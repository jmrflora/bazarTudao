// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jmrflora/bazarTudao/ent/envio"
	"github.com/jmrflora/bazarTudao/ent/itemordem"
	"github.com/jmrflora/bazarTudao/ent/predicate"
)

// EnvioQuery is the builder for querying Envio entities.
type EnvioQuery struct {
	config
	ctx        *QueryContext
	order      []envio.OrderOption
	inters     []Interceptor
	predicates []predicate.Envio
	withItens  *ItemOrdemQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EnvioQuery builder.
func (eq *EnvioQuery) Where(ps ...predicate.Envio) *EnvioQuery {
	eq.predicates = append(eq.predicates, ps...)
	return eq
}

// Limit the number of records to be returned by this query.
func (eq *EnvioQuery) Limit(limit int) *EnvioQuery {
	eq.ctx.Limit = &limit
	return eq
}

// Offset to start from.
func (eq *EnvioQuery) Offset(offset int) *EnvioQuery {
	eq.ctx.Offset = &offset
	return eq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eq *EnvioQuery) Unique(unique bool) *EnvioQuery {
	eq.ctx.Unique = &unique
	return eq
}

// Order specifies how the records should be ordered.
func (eq *EnvioQuery) Order(o ...envio.OrderOption) *EnvioQuery {
	eq.order = append(eq.order, o...)
	return eq
}

// QueryItens chains the current query on the "itens" edge.
func (eq *EnvioQuery) QueryItens() *ItemOrdemQuery {
	query := (&ItemOrdemClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(envio.Table, envio.FieldID, selector),
			sqlgraph.To(itemordem.Table, itemordem.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, envio.ItensTable, envio.ItensColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Envio entity from the query.
// Returns a *NotFoundError when no Envio was found.
func (eq *EnvioQuery) First(ctx context.Context) (*Envio, error) {
	nodes, err := eq.Limit(1).All(setContextOp(ctx, eq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{envio.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eq *EnvioQuery) FirstX(ctx context.Context) *Envio {
	node, err := eq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Envio ID from the query.
// Returns a *NotFoundError when no Envio ID was found.
func (eq *EnvioQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(1).IDs(setContextOp(ctx, eq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{envio.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eq *EnvioQuery) FirstIDX(ctx context.Context) int {
	id, err := eq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Envio entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Envio entity is found.
// Returns a *NotFoundError when no Envio entities are found.
func (eq *EnvioQuery) Only(ctx context.Context) (*Envio, error) {
	nodes, err := eq.Limit(2).All(setContextOp(ctx, eq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{envio.Label}
	default:
		return nil, &NotSingularError{envio.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eq *EnvioQuery) OnlyX(ctx context.Context) *Envio {
	node, err := eq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Envio ID in the query.
// Returns a *NotSingularError when more than one Envio ID is found.
// Returns a *NotFoundError when no entities are found.
func (eq *EnvioQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(2).IDs(setContextOp(ctx, eq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{envio.Label}
	default:
		err = &NotSingularError{envio.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eq *EnvioQuery) OnlyIDX(ctx context.Context) int {
	id, err := eq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Envios.
func (eq *EnvioQuery) All(ctx context.Context) ([]*Envio, error) {
	ctx = setContextOp(ctx, eq.ctx, "All")
	if err := eq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Envio, *EnvioQuery]()
	return withInterceptors[[]*Envio](ctx, eq, qr, eq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eq *EnvioQuery) AllX(ctx context.Context) []*Envio {
	nodes, err := eq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Envio IDs.
func (eq *EnvioQuery) IDs(ctx context.Context) (ids []int, err error) {
	if eq.ctx.Unique == nil && eq.path != nil {
		eq.Unique(true)
	}
	ctx = setContextOp(ctx, eq.ctx, "IDs")
	if err = eq.Select(envio.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eq *EnvioQuery) IDsX(ctx context.Context) []int {
	ids, err := eq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eq *EnvioQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eq.ctx, "Count")
	if err := eq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eq, querierCount[*EnvioQuery](), eq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eq *EnvioQuery) CountX(ctx context.Context) int {
	count, err := eq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eq *EnvioQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eq.ctx, "Exist")
	switch _, err := eq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eq *EnvioQuery) ExistX(ctx context.Context) bool {
	exist, err := eq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EnvioQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eq *EnvioQuery) Clone() *EnvioQuery {
	if eq == nil {
		return nil
	}
	return &EnvioQuery{
		config:     eq.config,
		ctx:        eq.ctx.Clone(),
		order:      append([]envio.OrderOption{}, eq.order...),
		inters:     append([]Interceptor{}, eq.inters...),
		predicates: append([]predicate.Envio{}, eq.predicates...),
		withItens:  eq.withItens.Clone(),
		// clone intermediate query.
		sql:  eq.sql.Clone(),
		path: eq.path,
	}
}

// WithItens tells the query-builder to eager-load the nodes that are connected to
// the "itens" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EnvioQuery) WithItens(opts ...func(*ItemOrdemQuery)) *EnvioQuery {
	query := (&ItemOrdemClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withItens = query
	return eq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Data time.Time `json:"data,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Envio.Query().
//		GroupBy(envio.FieldData).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eq *EnvioQuery) GroupBy(field string, fields ...string) *EnvioGroupBy {
	eq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EnvioGroupBy{build: eq}
	grbuild.flds = &eq.ctx.Fields
	grbuild.label = envio.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Data time.Time `json:"data,omitempty"`
//	}
//
//	client.Envio.Query().
//		Select(envio.FieldData).
//		Scan(ctx, &v)
func (eq *EnvioQuery) Select(fields ...string) *EnvioSelect {
	eq.ctx.Fields = append(eq.ctx.Fields, fields...)
	sbuild := &EnvioSelect{EnvioQuery: eq}
	sbuild.label = envio.Label
	sbuild.flds, sbuild.scan = &eq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EnvioSelect configured with the given aggregations.
func (eq *EnvioQuery) Aggregate(fns ...AggregateFunc) *EnvioSelect {
	return eq.Select().Aggregate(fns...)
}

func (eq *EnvioQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eq); err != nil {
				return err
			}
		}
	}
	for _, f := range eq.ctx.Fields {
		if !envio.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eq.path != nil {
		prev, err := eq.path(ctx)
		if err != nil {
			return err
		}
		eq.sql = prev
	}
	return nil
}

func (eq *EnvioQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Envio, error) {
	var (
		nodes       = []*Envio{}
		_spec       = eq.querySpec()
		loadedTypes = [1]bool{
			eq.withItens != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Envio).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Envio{config: eq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := eq.withItens; query != nil {
		if err := eq.loadItens(ctx, query, nodes,
			func(n *Envio) { n.Edges.Itens = []*ItemOrdem{} },
			func(n *Envio, e *ItemOrdem) { n.Edges.Itens = append(n.Edges.Itens, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (eq *EnvioQuery) loadItens(ctx context.Context, query *ItemOrdemQuery, nodes []*Envio, init func(*Envio), assign func(*Envio, *ItemOrdem)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Envio)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ItemOrdem(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(envio.ItensColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.envio_itens
		if fk == nil {
			return fmt.Errorf(`foreign-key "envio_itens" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "envio_itens" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (eq *EnvioQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eq.querySpec()
	_spec.Node.Columns = eq.ctx.Fields
	if len(eq.ctx.Fields) > 0 {
		_spec.Unique = eq.ctx.Unique != nil && *eq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eq.driver, _spec)
}

func (eq *EnvioQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(envio.Table, envio.Columns, sqlgraph.NewFieldSpec(envio.FieldID, field.TypeInt))
	_spec.From = eq.sql
	if unique := eq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eq.path != nil {
		_spec.Unique = true
	}
	if fields := eq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, envio.FieldID)
		for i := range fields {
			if fields[i] != envio.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eq *EnvioQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eq.driver.Dialect())
	t1 := builder.Table(envio.Table)
	columns := eq.ctx.Fields
	if len(columns) == 0 {
		columns = envio.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eq.sql != nil {
		selector = eq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eq.ctx.Unique != nil && *eq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eq.predicates {
		p(selector)
	}
	for _, p := range eq.order {
		p(selector)
	}
	if offset := eq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EnvioGroupBy is the group-by builder for Envio entities.
type EnvioGroupBy struct {
	selector
	build *EnvioQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (egb *EnvioGroupBy) Aggregate(fns ...AggregateFunc) *EnvioGroupBy {
	egb.fns = append(egb.fns, fns...)
	return egb
}

// Scan applies the selector query and scans the result into the given value.
func (egb *EnvioGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, egb.build.ctx, "GroupBy")
	if err := egb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EnvioQuery, *EnvioGroupBy](ctx, egb.build, egb, egb.build.inters, v)
}

func (egb *EnvioGroupBy) sqlScan(ctx context.Context, root *EnvioQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(egb.fns))
	for _, fn := range egb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*egb.flds)+len(egb.fns))
		for _, f := range *egb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*egb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := egb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EnvioSelect is the builder for selecting fields of Envio entities.
type EnvioSelect struct {
	*EnvioQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (es *EnvioSelect) Aggregate(fns ...AggregateFunc) *EnvioSelect {
	es.fns = append(es.fns, fns...)
	return es
}

// Scan applies the selector query and scans the result into the given value.
func (es *EnvioSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, es.ctx, "Select")
	if err := es.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EnvioQuery, *EnvioSelect](ctx, es.EnvioQuery, es, es.inters, v)
}

func (es *EnvioSelect) sqlScan(ctx context.Context, root *EnvioQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(es.fns))
	for _, fn := range es.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*es.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := es.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
